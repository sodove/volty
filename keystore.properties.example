# Copy to keystore.properties (gitignored) and fill in real values, OR set the
# matching env vars for CI: VOLTY_KEYSTORE_FILE / VOLTY_KEYSTORE_PASSWORD /
# VOLTY_KEY_ALIAS / VOLTY_KEY_PASSWORD.
#
# storeFile is resolved relative to the repository root.
storeFile=my-release.jks
storePassword=CHANGE_ME
keyAlias=CHANGE_ME
keyPassword=CHANGE_ME
